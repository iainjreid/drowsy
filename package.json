{
  "name": "drowsy",
  "scripts": {
    "prettier": "prettier --write *.{json,md} lib/*",
    "install": "lerna link",
    "prepare": "lerna run prepare",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "lerna": "^3.14.1",
    "prettier": "^1.17.1"
  },
  "jest": {
    "collectCoverage": true,
    "collectCoverageFrom": [
      "**/src/main/**/*.js"
    ],
    "moduleNameMapper": {
      "^drowsy/([a-z]+)$": "<rootDir>/packages/drowsy/$1",
      "^@chaff/drowsy-([a-z]+)$": "<rootDir>/packages/drowsy-$1/"
    }
  },
  "prettier": {
    "printWidth": 120,
    "proseWrap": "always"
  },
  "license": "MIT",
  "private": true
}
